<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fakehunters.backend.video.mapper.FrameAnalysisMapper">

    <resultMap id="frameAnalysisResultMap" type="fakehunters.backend.video.domain.FrameAnalysis">
        <id property="frameId" column="frame_id"/>
        <result property="resultId" column="result_id"/>
        <result property="frameNumber" column="frame_number"/>
        <result property="timestampSeconds" column="timestamp_seconds"/>
        <result property="isDeepfake" column="is_deepfake"/>
        <result property="confidenceScore" column="confidence_score"/>
        <result property="anomalyRegions" column="anomaly_regions"/>
        <result property="features" column="features"/>
    </resultMap>

    <insert id="insert" parameterType="fakehunters.backend.video.domain.FrameAnalysis"
            useGeneratedKeys="true" keyProperty="frameId" keyColumn="frame_id">
        INSERT INTO video_analysis_frame (
        result_id, frame_number, timestamp_seconds, is_deepfake, confidence_score, anomaly_regions, features
        ) VALUES (
        #{resultId},
        #{frameNumber},
        #{timestampSeconds},
        #{isDeepfake},
        #{confidenceScore},
        #{anomalyRegions}::jsonb,
        #{features}::jsonb
        )
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO video_analysis_frame (
        result_id, frame_number, timestamp_seconds, is_deepfake, confidence_score, anomaly_regions, features
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.resultId},
            #{item.frameNumber},
            #{item.timestampSeconds},
            #{item.isDeepfake},
            #{item.confidenceScore},
            #{item.anomalyRegions}::jsonb,
            #{item.features}::jsonb
            )
        </foreach>
    </insert>

    <select id="findById" resultMap="frameAnalysisResultMap">
        SELECT * FROM video_analysis_frame
        WHERE frame_id = #{frameId}
    </select>

    <select id="findByResultId" resultMap="frameAnalysisResultMap">
        SELECT * FROM video_analysis_frame
        WHERE result_id = #{resultId}
        ORDER BY frame_number ASC
    </select>

    <select id="findSuspiciousFramesByResultId" resultMap="frameAnalysisResultMap">
        SELECT * FROM video_analysis_frame
        WHERE result_id = #{resultId} AND is_deepfake = true
        ORDER BY confidence_score DESC
    </select>

    <select id="findByAnalysisIdAndFrameNumber" resultType="fakehunters.backend.video.domain.FrameAnalysis">
        SELECT vaf.*
        FROM video_analysis_frame vaf
        JOIN video_analysis_result var ON vaf.result_id = var.result_id
        WHERE var.analysis_id = #{analysisId}
        AND vaf.frame_number = #{frameNumber}
        LIMIT 1
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fakehunters.backend.image.mapper.ImageAnalysisArtifactMapper">

    <insert id="insert"
            parameterType="fakehunters.backend.image.domain.ImageAnalysisArtifact"
            useGeneratedKeys="true"
            keyProperty="artifactId">

        INSERT INTO image_analysis_artifact (
        artifact_uuid,
        job_id,
        artifact_stage,
        artifact_type,
        artifact_s3_key
        ) VALUES (
        #{artifactUuid, jdbcType=OTHER},
        #{jobId},
        #{artifactStage},
        #{artifactType},
        #{artifactS3Key}
        )
    </insert>

    <select id="findByJobId"
            parameterType="long"
            resultType="fakehunters.backend.image.domain.ImageAnalysisArtifact">
        SELECT *
        FROM image_analysis_artifact
        WHERE job_id = #{jobId}
        ORDER BY created_at ASC
    </select>

</mapper>
